# Photo folder

The active portrait is `profile.jpg`. Replace that file to change the homepage photo.

A square or slightly portrait-oriented image works best. At least 800 × 800 px is recommended. The crop position is controlled by `.portrait` in the theme CSS.
